# 引き継ぎ: Marginalia v1.8.1 公開完了 — 次は「実論文1本」をワークフローに載せる

**プロジェクト**: marginalia (Electron + React + TS Markdownエディタ + 論文ビルドシステム)
**保存日時**: 2026-06-11 朝（定時巡回による保存）
**ステータス**: v1.8.1 リリース・ローカルインストール済み。リポジトリはクリーン（この保存コミットを除く）
**main HEAD**: `ab8c580`（chore: v1.8.1）/ タグ `v1.8.1` / GitHub Release 6アセット公開済み / ローカル /Applications も v1.8.1

---

## 1. 到達点（直近2日で完了したこと）

### v1.7.0（共同編集追従）
- 注釈 author を設定の現在ユーザーに連動 / 外部変更の再読み込みで .mrgl も再読込（reloadNonce）/ 孤立検出を AnnotationProvider に常駐化

### v1.8.0（ギャップ解消 Phase A〜D + 論文 GUI 完結）
- **A-1** `.mrgl` 単独外部変更のサイレント自動反映（fileWatcher 3系統監視・後付け `.marginalia` 昇格・自己保存ガード）
- **A-2** authorId + 作成者色バッジ（.mrgl 2.1.0、2.0.0 後方互換）
- **B-1** 構造化履歴（author/authorId/detail 前後テキスト、512KBサイズ上限 truncateHistoryBySize）
- **B-2** 差分ビューに注釈ガター（mapAnnotationsToLines）
- **C-1** 論文ビルド通し検証で7バグ修正（外部フォルダのパス解決全般 / pandoc 3互換 / CSLReferences 全テンプレート追加 / インライン ref / 表キャプション / Figure 採番 / 前方参照3パス化）
- **C-2** 校閲レポート出力（注釈パネル「📋 レポート」→ `<name>.review.md`）
- **D** `.mrgl` git 3-way マージドライバ（`scripts/merge-mrgl.js`、変更vs削除は保持優先）
- **論文 GUI 完結**: ツールチェーン同梱（extraResources 約300KB）+ スタンドアロン論文フォルダ検出（直下のマニフェスト形 YAML）+「📄 論文プロジェクトを作成」雛形ボタン。出力は `<フォルダ>/mg_output/<fmt>/`

### v1.8.1（ドッグフード修正）
- 論文1本を実際に執筆して発見した2件: ①equation ディレクティブが LaTeX 中の `|x|` で分割壊れ → 最初の `|` のみで分割（lua + python 両パーサ）②Build パネルに「↻ 更新」ボタン（マニフェスト外部編集の反映）
- リリース時に GitHub 大規模障害（API 401）で publish のみ失敗 → 復旧監視で自動再実行して成功

### 実証成果物
- **`../marginalia-paper-demo/`** に完全な論文プロジェクト（5ページPDF+DOCX生成済み）: 図（matplotlib）・採番数式・表・文中相互参照・BibTeX引用5件・自己校閲注釈・校閲レポートまで全部入り。生きた見本として残してある

## 2. 検証状態
`tsc --noEmit` 0 / vitest **198件** / `eslint .` 0 error・**67 warning**（ベースライン。superseded 旧基準は68）/ build 成功 / CI緑 / E2E は CDP 実機駆動で全機能確認済み

## 3. 次のアクション（巡回指示: 実論文1本を実際に載せる）

**決定: 第一候補は FL（連合学習）実験の論文化**。根拠: pws-gpu3060 の bench コンテナで `fl_experiment`（pytorch 2.5.1-cuda12.4, :8101）が実際に稼働しており、最も具体的な研究材料が存在する。

1. **材料調査**: `ssh ubuntu@10.0.70.81`（または Tailscale 100.117.16.18）→ bench コンテナの fl_experiment の成果物（結果・図・ログ・既存メモ）を確認。実験の目的・手法・結果の素材がどこまで揃っているか棚卸し
2. **ユーザー確認（1問だけ）**: 「最初に載せる論文は FL 実験で良いか？ 投稿先（学会和文誌/国際会議/紀要）はどこを想定するか」— 題材と投稿先が決まらないと本物のテンプレート整備（IEEEtran 等）に進めないため
3. **論文プロジェクト生成**: 確定したら新フォルダに雛形生成 → 構成案（章立て）を素材から起こす → ユーザーレビュー（Marginalia の注釈で）
4. 並行整備（前セッションの評価で合意済みの優先順）: 投稿先テンプレート > 番号引用 CSL 同梱 > 論文雛形に CI（GitHub Actions で PDF 自動ビルド）+ `.gitattributes` を含める > 依存セットアップガイド

## 4. 残課題（任意・急ぎなし）
- lint warning 67（no-explicit-any 31 + react-hooks 系。本質的境界のため1件ずつ精査が必要）
- 旧 `ProjectPanel.tsx` は未使用（実体は `SidebarGallery.tsx`）— 削除候補
- `report-build-system` の分離は**やらないと評価済み**（CLI/MCP/GUI の3経路で自己完結済み。共同研究者・CI 需要が出たら再評価）
- localStorage→kv 実置換 / apps/web 実体化 / リモート古ブランチ掃除（v1.6.0 から持ち越し）

## 5. コンテキスト・メモ
- **E2E 手法**: dev 起動後 electron を `--remote-debugging-port=9223` で立て、素の WebSocket CDP（/tmp/marg-cdp.mjs 方式）で駆動。9222 は TiN が占有しているので使わない
- **GitHub Actions の publish が 401 で落ちたら**: ビルド成果物はランに残るので `gh run rerun <id> --failed` で publish だけ再実行できる（githubstatus.com で障害確認）
- ユーザーの構想: 「md を正として編集履歴を共有」「md がそのまま論文に」— 両方実装済み。今後は**実運用**フェーズ
- ユーザーの進め方: 短い指示で自走を期待。各段階でゲート（typecheck/test/build/lint）→ コミット&push → 機能はリリース（タグ→Release ワークフロー）まで
- 詳細はメモリ `marginalia-quality-roadmap.md` / `marginalia-blockize-monorepo.md`

## 6. 再開方法
このファイルを読んだら「3. 次のアクション」の 1〜2 から開始。論文題材の確定がクリティカルパスなので、材料調査とユーザーへの1問確認を最初に行うこと。
