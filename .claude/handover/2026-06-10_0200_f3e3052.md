# 引き継ぎ: Marginalia 機能ブロック化（ポート&アダプタ + モノレポ化）

**プロジェクト**: marginalia (Electron + React + TS Markdownエディタ)
**保存日時**: 2026-06-10（作業中断・すぐ再開予定）
**ステータス**: **M0〜M6a 完了・main にマージ済み・CI通過**。残は M6b 等（次段階）
**main HEAD**: `f3e3052`（Merge: 機能ブロック化, M0-M6a）/ 作業ツリー: main・クリーン（`.claude` のみ untracked）

---

## 1. 次のアクション（ユーザー選択待ち）

ユーザーが「M6a で M6 を区切る」を選択し main マージ済み。次は以下のどれかをユーザーに確認して着手:
- **A. M6b**（renderer を `packages/ui-react` へ分離 + Vite root 変更 + `package:mac` 検証）── 構造の完全化。**不可分・大規模・途中で動かない**ので新鮮なコンテキストで集中作業。手順はメモリ `marginalia-blockize-monorepo.md` に詳細あり
- **B. ポート機構のテスト追加**（`createElectronPorts` / 各Context のポート化）── 評価で弱かったテスト軸（4/10）の改善。低リスク・中価値
- **C. `createWebPorts` スタブ作成**── Web 対応の実証（注釈機能が electronAPI なしで動くことを示す）。ブロック化成果のデモ

迷ったら **B（ポートテスト）** が低リスクで価値が高い。

## 2. 達成したこと（M0〜M6a、全コミット main 統合済み）

ご要望「アプリと機能の混在解消・機能を他アプリに載せられるブロック化」を達成:
- **renderer(src/) の `window.electronAPI` 直叩きが 0**。全機能・全UIが **11個の PlatformPorts** 経由（annotations/fs/watcher/terminal/build/resources/bus/updater/shell/gallery/kv）
- 純粋コアを `packages/annotation-core`・`shared-types`・`ports` に分離
- electron 層を `apps/electron/` へ物理分離
- `createElectronPorts`（`src/adapters/electron/`）が既存 electronAPI を包む薄ブリッジ → **preload/main 無改修で Web/Tauri 対応の土台完成**

## 3. 残作業の詳細

- **M6b**: `src/`→`packages/ui-react/`、`vite.config` の `root` を src→apps/electron 直下 or packages 経由に、`index.html`/`index.tsx` 配置、`outDir` 調整、`apps/electron/package.json` 作成（真の workspace 化: main/build/deps 移設）。+ `npm run package:mac` で packaged 検証（dist パス `../../dist` が asar 内で正しいか、node-pty asarUnpack）
- **localStorage の kv 実置換**: KeyValueStore ポートは定義済み。実置換は async hydrate の初期描画ちらつきリスクで Web アダプタ実装時に延期（判断済み）

## 4. 検証状態（全工程で維持）
`tsc --noEmit` 0 / `vitest` 82件 / `vite build` 成功 / `eslint .` 0 error・68 warning（うち migration の any 2件は本質的レガシー境界）/ Electron 42 dev起動・ポート経由ロード確認。

## 5. コンテキスト・メモ
- **設計の核**: ①`createElectronPorts` ブリッジ（壊さない鍵）②旧パスは `@marginalia/*` への re-export スタブで import 温存 ③No-buildパッケージ（main がソース.ts を指す）+ package.json `exports: {".":..., "./*":...}` + npm workspace リンクで Vite/tsc 解決
- **ハマりどころ**（メモリに詳細）: Finder 重複コピー(`* 2.ts`)が tsc 汚染 / react-hooks v7 は `ports.fs` 等を依存に要求・ports未使用関数に入れると unnecessary warning / sed 行番号は import 追加でズレる / `eslint src electron` は packages を lint しないので `eslint .` に
- **ユーザーの進め方**: 「実行」「進め」と短く指示し自走を期待。各段階でコミット&pushしながら、各Mゲート（typecheck/test/build/lint）を必ず通す。自己改善ループ（粗を探し最良改善を1つ実行→繰り返す）を回す方針
- 詳細は自動メモリ `marginalia-blockize-monorepo.md` に完全記録（計画ファイル: `~/.claude/plans/goofy-inventing-stallman.md`）
- ブランチ `refactor/monorepo-blockize` はマージ済み（`git branch -d` で削除可）

## 6. 再開方法
`claude --continue` → 本ファイルとメモリを読み、上記 A/B/C をユーザーに確認して着手。
